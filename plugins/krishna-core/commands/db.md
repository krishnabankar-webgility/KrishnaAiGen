---
description: 'Local SQL Server operations: restore a .bak, attach, query, list or drop databases'
argument-hint: '[e.g. "restore customer bak from Reference/UD-33334"]'
---

Delegate this request to the **`db-automation`** subagent using the Agent tool.

The subagent must load these skills before acting: `db-restore`.

If the request below is empty, ask what is needed rather than guessing.

## Request

$ARGUMENTS

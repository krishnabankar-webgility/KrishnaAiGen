---
description: 'Jira UD work: create issues/subtasks, Story Points + Priority Rank, Original Estimate, worklogs, Done vs RFT, QA Testing comment, sprints'
argument-hint: '[what to do, e.g. "mark UD-33334 RFT" or "create a story for CUST-123"]'
---

Delegate this request to the **`jira-automation`** subagent using the Agent tool.

The subagent must load these skills before acting: `jira-workflow`.

If the request below is empty, ask what is needed rather than guessing.

## Request

$ARGUMENTS

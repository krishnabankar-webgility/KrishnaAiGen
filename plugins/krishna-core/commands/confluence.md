---
description: 'Confluence pages: read, search, create, update, publish a report or mirror a QA comment'
argument-hint: '[page or action]'
---

Delegate this request to the **`confluence-automation`** subagent using the Agent tool.

The subagent must load these skills before acting: `confluence-workflow`.

If the request below is empty, ask what is needed rather than guessing.

## Request

$ARGUMENTS
